{
  "permissions": {
    "allow": [
      "Bash(git add *)",
      "Bash(git commit -m ' *)",
      "Bash(git push *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "git add . && (git diff --cached --quiet || (msg=$(git diff --cached --name-only | head -5 | tr '\\n' ' ' | sed 's/ $//'); git commit -m \"auto: update $msg\" && git push)) 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Committing and pushing..."
          }
        ]
      }
    ]
  }
}
