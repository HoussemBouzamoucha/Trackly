{
  "permissions": {
    "allow": [
      "Bash(git add *)",
      "Bash(git commit -m ' *)",
      "Bash(git push *)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path // \"file\"' 2>/dev/null); fname=$(basename \"$file\"); git add . && (git diff --cached --quiet || (git commit -m \"auto: update $fname\" && git push)) 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Committing and pushing..."
          }
        ]
      }
    ]
  }
}
